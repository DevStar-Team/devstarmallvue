server:
  port: 8080

spring:
  cloud:
    config:
      uri: http://config.devstar.me:8888
      name: devStarMallVue
      headers:
        application-name: devStarMallVue
        application-password: "devStarMallVue"