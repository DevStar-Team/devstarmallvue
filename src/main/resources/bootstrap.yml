server:
  port: 8080

spring:
  cloud:
    config:
      uri: http://13.124.204.199:8888
      name: devStarMallVue
      headers:
        application-name: devStarMallVue
        application-password: "devStarMallVue"